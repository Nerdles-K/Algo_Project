# SynchPlay 项目落地计划书（算法课精简版）

## 1. 项目定位与目标
**SynchPlay** 是一款基于社交图谱的视频推荐引擎,类似于youtube 。其核心目标是解决现代算法导致的“信息茧房”问题，通过分析用户的社交圈（好友看了什么）来提供更具社交共鸣的视频推荐 。

本项目将重点实现：

- **异构图构建**：建立“用户-视频”社交模型 。
- **双层推荐逻辑**：基于图遍历的候选集召回 + 基于数学公式的精准排序 。
- **茧房深度检测**：利用局部聚类系数（LCC）量化社交圈的封闭性 。

## 2. 简化版技术架构

为了节省时间，我们将架构从“Web服务”简化为“本地算法引擎”：

- **运行平台**：Java 控制台应用 (Console Application)。

- **开发工具**：VS Code + Java Extension Pack。

- **数据流**：

  1. **数据层**：使用 Python 脚本从原始数据集中抽取 500 个节点，导出为微型 CSV 文件 。
  2. **核心层**：Java 编写的邻接表图结构及算法逻辑 。
  3. **展示层**：通过控制台 (Terminal) 打印推荐结果和指标 。

## 3. 三人小组保姆级分工

针对代码基础薄弱的情况，我们将任务拆解为三个**互不干扰**的模块。

### 👨‍💻 成员 A：数据基建与图对象构建 (基础岗)
**核心任务**：把 CSV 里的文字变成 Java 内存里的“图” 。
- **具体内容**：

- **具体内容**：
   1. **Python 抽样**：运行 Python 脚本，从 SNAP 和 Kaggle 数据集中随机切出 100 个用户和 400 个视频，生成 `mini_nodes.csv` 和 `mini_edges.csv` 。
   2. **Java 类定义**：在 VS Code 中创建 `Node`（节点）和 `Graph`（图）类。`Node` 里要有一个 `List` 用来存它的邻居。
   3. **数据读取**：写一个简单的加载类，把 CSV 里的每一行读出来，转换成 Java 的 `Node` 对象，并建立连接。

- **交付物**：一个可以被调用的 `Graph` 对象，另外两位组员直接从你这里拿数据。

### 👨‍💻 成员 B：寻路与召回算法实现 (逻辑岗)
**核心任务**：在图中“跑腿”，找出候选视频 。
- **具体内容**：

- **具体内容**：
   1. **实现 BFS 遍历**：从目标用户开始，向外走 2-3 层（即：用户 -> 好友 -> 视频），把这些视频全部抓出来。
   2. **最短路径逻辑**：如果想拿高分，可以尝试实现 Dijkstra 算法，根据视频的发布时间或观看次数计算“路径权重” 。
   3. **异常处理**：确保代码不会死循环。使用一个 `Set` 来记录已经走过的节点，防止程序跑飞。

- **交付物**：一个方法，输入用户 ID，输出一个未经排序的 `List<Video>`。

### 👨‍💻 成员 C：打分公式与指标计算 (数学岗)
**核心任务**：给视频排队，并算出“茧房”有多深 。
- **具体内容**：

- **具体内容**：
  1. **实现 PageRank 逻辑**：编写一个简单的循环，给每个视频算一个“全局热度分” 。
  2. **综合打分公式**：将成员 B 找出的视频，代入公式：$最终分 = (\alpha \times 路径距离) + (\beta \times 热度分)$ 。
  3. **茧房检测 (LCC)**：计算目标用户的邻居们互相认识的比例。如果比例过高，说明用户处于“茧房”中 。

- **交付物**：最终的排序结果输出功能，以及打印出生态健康度。

## 4. 进度安排 (12 周压缩版)

| **阶段**             | **周次** | **关键交付物**                                               |
| -------------------- | -------- | ------------------------------------------------------------ |
| **阶段 1：环境搭建** | W1-W3    | 成员 A 导出小型数据集，三人配置好 VS Code 开发环境。         |
| **阶段 2：模块开发** | W4-W8    | 成员 B 写完寻路逻辑，成员 C 写完打分公式，各自在小数据集上跑通。 |
| **阶段 3：合体演示** | W9-W12   | 将三个类合并到 `Main` 方法，进行控制台 Demo 演示，编写简单的实验报告。 |

## 5. 给代码薄弱同学的建议

1. **不要怕报错**：VS Code 的 Java 插件会用红色波浪线标出大部分错误，鼠标放上去通常有“快速修复”建议。
2. **先写死，再写活**：如果读 CSV 觉得难，可以先在 Java 里手动创建 5 个节点连接起来，测试自己的算法对不对。
3. **多用 `System.out.println`**：在算法的每一个循环里都打印一下结果，这样你就知道代码运行到哪一步了。

这个计划书将你们的 presentation 构想 转化为了最易操作的代码任务，重点突出了算法课要求的图论核心逻辑 。